const form = (state, action) => {
  switch (action.type) {
    // case 'DECREMENT_COUNTER':
    //   return state - 1;
    // case 'INCREMENT_COUNTER':
    //   return state + 1;
    default:
      return state;
  }
}

export default form;
